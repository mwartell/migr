[project]
name = "migr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "matt wartell", email = "matt.wartell@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "alembic>=1.19.2",
    "psycopg[binary]>=3.3.5",
]

[project.scripts]
migr = "migr:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.6",
]
